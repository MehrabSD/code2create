{
  "scenario": 14,
  "name": "Scale Correctness \u2014 15 Reps, 10 Proposals",
  "description": "Large dataset with 15 reps and 10 proposals. Tests that engine runs to completion correctly, produces valid JSON, all IDs exist in input, and no duplicates.",
  "expected_output": {
    "note": "Dynamic \u2014 evaluate that output is valid JSON, IDs exist, no duplicates, at least 1 proposal and 1 supporter, and alliances are bidirectional."
  },
  "scoring": {
    "full_credit": "valid output, correct decisions at scale",
    "partial_credit": "valid output but suboptimal decisions",
    "zero": "crash or invalid output"
  }
}